# llm-gis

Read `AGENTS.md` for architecture, commands, operator mode and source handling.
For work started in another folder, read `docs/llm/GLOBAL_WORKFLOW.md` and use
the global launcher with host paths and the original caller working directory.

## Adding new capabilities

When new modules are added to this workspace (remote sensing, vision, AI, etc.), add the module's technical context to `.claude/skills/hot-start/SKILL.md`.
